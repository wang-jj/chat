package com.example.dell.chat.db;

import java.util.Map;

import org.greenrobot.greendao.AbstractDao;
import org.greenrobot.greendao.AbstractDaoSession;
import org.greenrobot.greendao.database.Database;
import org.greenrobot.greendao.identityscope.IdentityScopeType;
import org.greenrobot.greendao.internal.DaoConfig;

import com.example.dell.chat.bean.User;
import com.example.dell.chat.bean.PersonalState;

import com.example.dell.chat.db.UserDao;
import com.example.dell.chat.db.PersonalStateDao;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.

/**
 * {@inheritDoc}
 * 
 * @see org.greenrobot.greendao.AbstractDaoSession
 */
public class DaoSession extends AbstractDaoSession {

    private final DaoConfig userDaoConfig;
    private final DaoConfig personalStateDaoConfig;

    private final UserDao userDao;
    private final PersonalStateDao personalStateDao;

    public DaoSession(Database db, IdentityScopeType type, Map<Class<? extends AbstractDao<?, ?>>, DaoConfig>
            daoConfigMap) {
        super(db);

        userDaoConfig = daoConfigMap.get(UserDao.class).clone();
        userDaoConfig.initIdentityScope(type);

        personalStateDaoConfig = daoConfigMap.get(PersonalStateDao.class).clone();
        personalStateDaoConfig.initIdentityScope(type);

        userDao = new UserDao(userDaoConfig, this);
        personalStateDao = new PersonalStateDao(personalStateDaoConfig, this);

        registerDao(User.class, userDao);
        registerDao(PersonalState.class, personalStateDao);
    }
    
    public void clear() {
        userDaoConfig.getIdentityScope().clear();
        personalStateDaoConfig.getIdentityScope().clear();
    }

    public UserDao getUserDao() {
        return userDao;
    }

    public PersonalStateDao getPersonalStateDao() {
        return personalStateDao;
    }

}
